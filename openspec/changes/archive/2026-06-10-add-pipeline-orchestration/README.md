# add-pipeline-orchestration

Wire ingestion -> analysis -> target generation into one end-to-end runnable pipeline (signal -> thesis -> target -> watchlist) with per-stage resilience, composing existing layers with stubs
